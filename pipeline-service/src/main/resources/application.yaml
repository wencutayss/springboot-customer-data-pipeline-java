server:
  port: 8000

spring:
  application:
    name: pipeline-service
  datasource:
    url: jdbc:postgresql://postgres:5432/customer_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true